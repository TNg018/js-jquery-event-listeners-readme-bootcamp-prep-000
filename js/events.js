//define functions here

$(document).ready(function(){

// call functions here

});

function getIt() {
  $(paragraph).on('keydown', function(key) {
    if(key.which == pressed)
    return key
  });  
}